import React from 'react';

import './Header.scss';

const Header: React.FunctionComponent = () => {
  return (
    <header>
      <div>
        <img src="./assets_logo.png" alt="logo" />
      </div>
    </header>
  );
}

export default Header;
